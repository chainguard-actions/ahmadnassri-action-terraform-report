# ---------------------------------------------------- #
# Note: this file originates in template-action-node #
# ---------------------------------------------------- #

name: push

on:
  - push
  - workflow_dispatch

permissions:
  contents: write
  statuses: write
  packages: write
  pull-requests: write

jobs:
  main:
    uses: ahmadnassri/actions/.github/workflows/push-action-node.yml@8b3e5adb19d5eb57b372a483ef21434fa7b408d4 # master
    secrets: inherit
    permissions:
      contents: write
      statuses: write
      packages: write
      pull-requests: write
