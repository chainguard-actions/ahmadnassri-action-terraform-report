# ---------------------------------------------------- #
# Note: this file originates in template-action-node #
# ---------------------------------------------------- #

name: push

on:
  - push
  - workflow_dispatch

permissions:
  contents: read

jobs:
  main:
    uses: ahmadnassri/actions/.github/workflows/push-action-node.yml@2b10e2b75222499bd27919877ba73a30eb15996b # master
    secrets: inherit
    permissions:
      contents: write
      statuses: write
      packages: write
      pull-requests: write
