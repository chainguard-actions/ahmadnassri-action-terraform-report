name: Test basic usage - terraform report with plan files
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/1/merge
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create terraform plan text file
        run: |
          mkdir -p /tmp/terraform-output
          cat > /tmp/terraform-output/plan.txt << 'EOF'
          Terraform used the selected providers to generate the following execution
          plan. Resource actions are indicated with the following symbols:
            + create

          Terraform will perform the following actions:

            # aws_s3_bucket.example will be created
            + resource "aws_s3_bucket" "example" {
                + bucket = "my-test-bucket"
                + id     = (known after apply)
              }

          Plan: 1 to add, 0 to change, 0 to destroy.
          EOF

      - name: Create terraform plan JSON file
        run: |
          cat > /tmp/terraform-output/plan.json << 'EOF'
          {
            "format_version": "1.0",
            "terraform_version": "1.5.0",
            "variables": {},
            "planned_values": {
              "root_module": {
                "resources": [
                  {
                    "address": "aws_s3_bucket.example",
                    "mode": "managed",
                    "type": "aws_s3_bucket",
                    "name": "example",
                    "provider_name": "registry.terraform.io/hashicorp/aws",
                    "schema_version": 0,
                    "values": {
                      "bucket": "my-test-bucket"
                    }
                  }
                ]
              }
            },
            "resource_changes": [
              {
                "address": "aws_s3_bucket.example",
                "mode": "managed",
                "type": "aws_s3_bucket",
                "name": "example",
                "provider_name": "registry.terraform.io/hashicorp/aws",
                "change": {
                  "actions": ["create"],
                  "before": null,
                  "after": {
                    "bucket": "my-test-bucket"
                  },
                  "after_unknown": {
                    "id": true
                  }
                }
              }
            ],
            "configuration": {
              "root_module": {}
            }
          }
          EOF

      - name: Run terraform report action
        id: action
        continue-on-error: true
        uses: .
        with:
          github-token: fake-token-for-testing
          terraform-text: /tmp/terraform-output/plan.txt
          terraform-json: /tmp/terraform-output/plan.json
          show-header: "true"
          show-footer: "true"
          show-plan: "true"
          show-diff: "false"
          remove-stale-reports: "false"

      - name: Assert action ran (may fail due to no real GitHub API)
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          # The action is expected to fail when trying to post to GitHub API
          # with a fake token, but it should at least start and process inputs.
          # We verify the action was invoked (outcome is set).
          if [ -z "${{ steps.action.outcome }}" ]; then
            echo "FAIL: action outcome was not set"
            exit 1
          fi
          echo "PASS: action was invoked with outcome: ${{ steps.action.outcome }}"
