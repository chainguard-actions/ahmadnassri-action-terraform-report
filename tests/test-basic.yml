name: Test basic usage with terraform plan changes
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Copy fixture files to workspace
        run: |
          cp "$GITHUB_WORKSPACE/tests/fixtures/terraform-with-changes.json" /tmp/terraform.json
          cp "$GITHUB_WORKSPACE/tests/fixtures/terraform-plan.txt" /tmp/terraform.txt

      - name: Run action with terraform plan containing changes
        id: action
        uses: .
        with:
          terraform-text: /tmp/terraform.txt
          terraform-json: /tmp/terraform.json
          show-plan: "true"
          show-diff: "false"
          show-header: "true"
          show-footer: "true"
          show-no-changes: "true"
          remove-stale-reports: "false"

      - name: Assert action completed successfully
        run: |
          echo "Action completed successfully (exited 0 as expected for non-PR context)"
          echo "PASS"
